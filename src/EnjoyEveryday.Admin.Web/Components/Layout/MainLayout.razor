@inherits LayoutComponentBase
@using Microsoft.FluentUI.AspNetCore.Components

<FluentLayout>
    <!-- MOBILE ONLY HEADER -->
    <FluentLayoutItem Area="LayoutArea.Header" class="mobile-header">
        <FluentStack VerticalAlignment="VerticalAlignment.Center" HorizontalGap="15">
            <FluentLayoutHamburger />
            <div style="display: flex; gap: 8px; align-items: center;">
                <div style="width: 24px; height: 24px; border-radius: 6px; background: var(--accent-light); color: var(--accent-dark); display: grid; place-items: center; font-size: 14px;">
                    ✨
                </div>
                <div style="font-weight: 750; font-size: 14px; letter-spacing: -0.5px; color: var(--text);">EnjoyEveryday</div>
            </div>
        </FluentStack>
    </FluentLayoutItem>

    <FluentLayoutItem Area="LayoutArea.Navigation" Width="260px">
        <NavMenu />
    </FluentLayoutItem>

    <FluentLayoutItem Area="LayoutArea.Content">
        @* <header class="topbar">

   <div>

        <div id="breadcrumb" class="breadcrumb">Little Stars Daycare / Settings</div>

        <h1 id="pageTitle" class="page-title">Settings</h1>

    </div>


    <div class="top-actions">

        <button id="notificationButton" class="icon-button">

            ♧

            <span class="notification"></span>

        </button>


        <button id="askAIButton" class="primary">

            ✦ Ask Experience AI

        </button>

    </div>

</header> *@
        @Body
    </FluentLayoutItem>
</FluentLayout>

<FluentProviders />
